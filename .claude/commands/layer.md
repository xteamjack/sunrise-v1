---
description: Build the next layer following the cookbook module.
argument-hint: <module number and goal>
---
Build the layer described in: $ARGUMENTS.
First make a short plan and show it. Then build the smallest working version,
following CLAUDE.md. When done, run the validate step for this layer and then /eval.
