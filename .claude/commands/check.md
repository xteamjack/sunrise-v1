---
description: Run the validate scripts for the current layer.
---
Run the validate script for the layer we are working on (for example
`uv run --project apps/support-agent python -m data.validate`) and report PASS or FAIL for each check.
